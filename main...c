
	
#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

	tong += arr[row][col];
	}
int main(int argc, char *argv[]) {
	int tong=0;
	int arr[4][30];
	int row,col;
	
	for(col=0;col<30;col++);
	tong =0;
	for(row=0;row<4;row++);
	{ printf("nhap diem tung mon cua SV\n");
	scanf("%d",&arr[col][row]);
	printf("tong=%d",tong);
}
	
